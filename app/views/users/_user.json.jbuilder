json.extract! user, :id, :user_type, :first_name, :last_name, :email, :username, :password, :home_state, :major, :school, :bio, :image_filename, :resume_filename, :linkedin, :created_at, :updated_at
json.url user_url(user, format: :json)