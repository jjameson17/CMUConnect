<%= form_for(@student_group_association) do |f| %>
  <% if @student_group_association.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_group_association.errors.count, "error") %> prohibited this student_group_association from being saved:</h2>

      <ul>
      <% @student_group_association.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_group_id %><br>
    <%= f.number_field :student_group_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
